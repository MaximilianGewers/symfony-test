{% extends 'base.html.twig' %}

{% block body %}
    <h1>Personcompanyrolejoin</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ personCompanyRoleJoin.id }}</td>
            </tr>
            <tr>
                <th>Role</th>
                <td>{{ personCompanyRoleJoin.role }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('personcompanyrolejoin_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('personcompanyrolejoin_edit', { 'id': personCompanyRoleJoin.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
